BackTracking techniques:

Always draw the tree and decide how you wanna manipulat forloop i

For Permutation, let the for loop to run with i = 0 and add it to track only if the current value
does not exists already

For combinations (making change, coins), manipulate i = start with current + 1